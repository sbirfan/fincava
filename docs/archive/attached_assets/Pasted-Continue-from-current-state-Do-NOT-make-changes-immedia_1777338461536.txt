Continue from current state. Do NOT make changes immediately.

First perform ALL preflight checks. Only proceed if all checks pass and no conflicts are found. If anything is inconsistent, STOP and report.

---

T4 — Extract Scoring + Document Prompts to Registry (AI1 + AI1-EXT)

Task ID: AI1 + AI1-EXT
Type: Critical Code Task — EP8 Compliance
Priority: CRITICAL PATH — sole remaining Phase 1 code blocker
Dependency: T1 (API key verified)

---

OBJECTIVE

Extract the hardcoded scoring prompt from services/scoring-service.ts into a centralized registry file:

src/config/scoring-prompts.ts

Optionally extract document prompt if present.

This is a PURE REFACTOR — ZERO behavior change.

---

PRE-FLIGHT (MANDATORY)

1. Confirm API working (T1 already validated)

2. Locate scoring prompt:
   Read: src/services/scoring-service.ts
   Confirm prompt exists as hardcoded string (around line ~40)

3. Check for document prompt:
   Run:
   grep -r "DOCUMENT_PROMPT|document.*prompt|generateDocument" src/ --include="*.ts"

4. Confirm registry file does NOT exist:
   ls src/config/

   If scoring-prompts.ts exists:
   → STOP and report (partial implementation)

5. Ensure clean TypeScript baseline:
   pnpm tsc --noEmit

---

STEP 1 — CREATE PROMPT REGISTRY

Create file:

src/config/scoring-prompts.ts

Add:

/**

* Fincava Prompt Registry
* EP8: Prompts are configuration, not logic
  */

IMPORTANT RULES:

* Copy prompt EXACTLY as-is
* DO NOT change formatting, spacing, indentation, or line breaks
* DO NOT modify a single character
* Preserve template string backticks (`...`) if used
* Do NOT convert to quotes

Add:

export const SCORING_PROMPT_V0 = `[PASTE EXACT PROMPT FROM scoring-service.ts VERBATIM]`;

export const SCORING_PROMPT = SCORING_PROMPT_V0;

// Document prompt (ONLY if found — otherwise leave commented)
// export const DOCUMENT_PROMPT_V0 = `[EXACT DOCUMENT PROMPT]`;
// export const DOCUMENT_PROMPT = DOCUMENT_PROMPT_V0;

---

STEP 2 — UPDATE SCORING SERVICE

Open:

src/services/scoring-service.ts

1. Remove hardcoded prompt string
2. Add import (adjust path if needed):

import { SCORING_PROMPT } from '../config/scoring-prompts';

3. Replace all usages with SCORING_PROMPT

CRITICAL:

* Do NOT modify any other logic
* Do NOT touch VALID_AI_PATHWAYS
* Do NOT change error handling
* Do NOT change function structure

---

STEP 3 — HANDLE DOCUMENT SERVICE

Run:

cat src/services/document-service.ts 2>/dev/null || echo "FILE NOT FOUND"

IF file exists AND contains hardcoded prompt:

* Copy prompt EXACTLY
* Uncomment DOCUMENT_PROMPT_V0
* Add prompt verbatim
* Replace usage with import

IF file does NOT exist or has no prompt:

* Do NOTHING
* Do NOT create placeholders

---

STEP 4 — VALIDATION

1. Type check:

pnpm tsc --noEmit

Expected: 0 errors

---

2. Ensure no prompts remain in services:

grep -r "You are|Analyze|Score|Generate" src/services/ --include="*.ts"

Expected:

* No prompt strings

---

3. Confirm correct usage:

grep -r "SCORING_PROMPT" src/

Expected:

* scoring-prompts.ts (definition)
* scoring-service.ts (import)

---

4. Ensure no duplicate definitions:

grep -r "SCORING_PROMPT_V0" src/

Expected:

* ONLY in scoring-prompts.ts

---

5. Visual validation:

Compare original prompt vs extracted prompt

They MUST be IDENTICAL

---

STEP 5 — SMOKE TEST

Trigger:

POST /api/suppliers/onboard

Then check:

SELECT score_data FROM ai_outputs ORDER BY created_at DESC LIMIT 1;

Expected:

* Valid JSON
* export_readiness_score present

---

ARCHITECTURAL CONSTRAINTS

* EP2: prompts = configuration
* EP8: centralized registry
* ZERO behavior change
* No logic added to config file

---

BACK-OUT PLAN

git revert HEAD

or:

git checkout HEAD~1 -- src/services/scoring-service.ts
rm src/config/scoring-prompts.ts

---

OUTPUT

Return:

1. Whether prompt was found and extracted
2. Whether document prompt exists
3. TypeScript result
4. Validation results
5. Confirmation prompt is identical
6. Smoke test result

If any issue:
→ STOP and report

---

END
