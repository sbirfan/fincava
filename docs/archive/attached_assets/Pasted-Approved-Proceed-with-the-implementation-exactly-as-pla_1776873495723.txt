Approved.

Proceed with the implementation exactly as planned, with the following REQUIRED constraints:

---

IMPORT PATH

* Use the correct relative path:
  ../../../../lib/config/thresholds

---

CORE INVARIANT — WRITE ORDER

* evaluation → transition → supplier update

* fromState MUST always be read from the persisted supplier BEFORE any update

* Never update suppliers without a corresponding evaluation snapshot

---

TRANSACTION

* Wrap ALL operations in a single DB transaction:

  * evaluation insert
  * transition insert (if any)
  * supplier update

* If any step fails → rollback everything

---

EVALUATION EDGE CASES

* If supplier does not exist → throw NotFoundError

* If no ai_outputs row exists:
  → throw NotFoundError
  → DO NOT insert evaluation
  → DO NOT update supplier
  → DO NOT insert transition

* If compliance_docs row is missing:
  → treat all required fields as missing
  → eligibilityStatus = FAIL

* If ai_outputs.pathway is null or invalid:
  → set pathway = null (do not throw)

---

EVALUATION LOGIC CLARIFICATION

* graduationPathway MUST be derived from ai_outputs.pathway
* This value is free-text and must be validated against enum ('A','B','C','D')
* If not valid → set pathway = null (do not throw)

---

IDEMPOTENCY

* ALWAYS insert exactly ONE supplier_evaluations row per evaluateSupplier() call

* This insert MUST occur inside the transaction (never outside)

* Compare against previous evaluation:

  * If (eligibilityStatus, commercialScore, sellableStatus) unchanged:
    → DO NOT insert transition
  * If no previous evaluation exists:
    → treat as changed and insert transition

---

TRANSITION RULES

* Forward transitions:
  NOT_READY → ELIGIBLE → SELLABLE → PUBLISHED

* SYSTEM actor:

  * cannot perform backward transitions
  * MUST include evaluationId

* ADMIN / FOUNDER:

  * backward transitions allowed
  * justification ALWAYS required (no exceptions)

* Global rule:
  actor !== 'SYSTEM' → justification REQUIRED before any DB write

---

EVALUATION ↔ TRANSITION LINKAGE

* SYSTEM transitions:
  → MUST include evaluationId referencing the evaluation row created in the same transaction

* ADMIN / FOUNDER transitions:
  → include evaluationId if available (within evaluation flow)
  → otherwise (manual override), evaluationId may be null

* This must be handled explicitly inside transitionTo()

---

THRESHOLDS

* Read ONLY from THRESHOLDS config
* DO NOT inline any numeric values

---

SENTRY

* Breadcrumbs required for every transition

* Integration must be optional:
  → MUST NOT throw if Sentry is not installed
  → fallback to console.debug

---

SAFETY

* Do NOT modify routes
* Do NOT call this service yet (wiring happens in Ticket 1.6)
* Do NOT modify any schema files

---

Proceed.
