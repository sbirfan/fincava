STOP — ADD OBSERVABILITY + GUARDRAILS (MINIMAL, SAFE)

Do NOT modify schema.
Do NOT change business logic.
Do NOT introduce external services.
This is visibility only.

---

# OBJECTIVE

Add:

1. Startup env visibility
2. Guardrail logging for critical flows
3. Lightweight notifications via existing logger

---

# PART 1 — STARTUP ENV CHECK

File:
artifacts/api-server/src/index.ts (or server entry)

---

## Add after server boot:

if (!process.env.FINCAVA_COMPANY_ID) {
logger.warn({
event: "CONFIG_MISSING",
key: "FINCAVA_COMPANY_ID"
}, "FINCAVA_COMPANY_ID not set — falling back to name lookup");
} else {
logger.info({
event: "CONFIG_LOADED",
key: "FINCAVA_COMPANY_ID",
value: process.env.FINCAVA_COMPANY_ID
}, "FINCAVA_COMPANY_ID loaded");
}

---

# PART 2 — PRODUCT CREATION GUARDRAILS

File:
admin.ts → create-product handler

---

## Add BEFORE insert:

if (!supplier) {
logger.error({
event: "PRODUCT_CREATE_SUPPLIER_MISSING",
supplierId
}, "Supplier not found during product creation");
}

---

## Add AFTER successful creation:

logger.info({
event: "PRODUCT_CREATED",
supplierId,
productId
});

---

# PART 3 — ORDER CREATION GUARDRAILS

File:
orders.ts → order creation flow

---

## Add BEFORE insert:

if (!product.supplierId) {
logger.warn({
event: "ORDER_PRODUCT_NO_SUPPLIER",
productId: product.id
}, "Order attempted with product missing supplier_id");
}

---

## Add AFTER order creation:

logger.info({
event: "ORDER_CREATED",
orderId,
supplierId: product.supplierId,
totalUSD
});

---

# PART 4 — SUPPLIER ONBOARDING SIGNAL

File:
supplier onboarding handler

---

Add AFTER success:

logger.info({
event: "SUPPLIER_ONBOARDED",
supplierId
});

---

# PART 5 — OPTIONAL HIGH-SIGNAL ALERTS

Add ONLY for critical anomalies:

* COMPANY_RESOLUTION_FAILED → already exists
* ORDER_PRODUCT_NO_SUPPLIER → warn
* CONFIG_MISSING → warn

Do NOT add alerts for normal events.

---

# PART 6 — TYPECHECK

pnpm --filter @workspace/api-server exec tsc --noEmit

---

# PART 7 — VALIDATION

Trigger:

1. Start server → check CONFIG_LOADED / CONFIG_MISSING
2. Create supplier → SUPPLIER_ONBOARDED
3. Create product → PRODUCT_CREATED
4. Create order → ORDER_CREATED

---

# OUTPUT

1. Startup check added: YES/NO
2. Product guardrails added: YES/NO
3. Order guardrails added: YES/NO
4. Supplier signal added: YES/NO
5. Typecheck: PASS/FAIL

---

# CRITICAL RULES

* Do NOT add external notification services
* Do NOT send emails/SMS
* Do NOT block execution on these checks
* Logs only — non-blocking

---

END
