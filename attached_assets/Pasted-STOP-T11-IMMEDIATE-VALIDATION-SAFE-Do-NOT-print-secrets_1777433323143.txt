STOP — T11 IMMEDIATE VALIDATION (SAFE)

Do NOT print secrets.
Do NOT hardcode BACKUP_SECRET in logs or files.
Read secrets only from environment.

---

# OBJECTIVE

Verify backup endpoint, storage upload, retention, and restore capability.

---

# PREFLIGHT

* Confirm BACKUP_SECRET is set in environment
* Confirm DEFAULT_OBJECT_STORAGE_BUCKET_ID is set
* Server is running and reachable

---

# STEP 1 — MANUAL ADMIN TRIGGER

1. Obtain admin JWT (existing admin user)
2. Call:

POST /api/admin/backup/run

Headers:
Authorization: Bearer <admin_jwt>

Expected:

* HTTP 200
* JSON: { success: true, file: "<filename>" }

Store filename as LATEST_FILE

---

# STEP 2 — SECRET TRIGGER (CRON SIMULATION)

Call the same endpoint using header auth:

POST /api/admin/backup/run

Headers:
X-Backup-Token: (read from process.env.BACKUP_SECRET)

Expected:

* HTTP 200
* JSON: { success: true, file: "<filename>" }

Store filename as LATEST_FILE_2

---

# STEP 3 — OBJECT STORAGE VERIFICATION

Using objectStorageClient:

* List files with prefix "fincava_"
* Confirm:

  * LATEST_FILE exists
  * LATEST_FILE_2 exists
  * File size > 0 bytes

---

# STEP 4 — RETENTION CHECK

* Count files with prefix "fincava_"
* If > 7:

  * Confirm only 7 most recent remain
  * Older files deleted

---

# STEP 5 — DOWNLOAD & RESTORE TEST (MANDATORY)

1. Download LATEST_FILE from object storage to /tmp
2. Create temp DB (e.g., test_restore)
3. Run:

pg_restore -d test_restore /tmp/<LATEST_FILE>

4. Verify:

* Tables exist
* Row counts > 0
* No restore errors

---

# STEP 6 — ERROR CHECK

* No unhandled exceptions
* No console.* usage
* Logger entries present for:

  * upload
  * retention deletes (if any)
  * errors (if any)

---

# OUTPUT

1. Admin trigger: PASS/FAIL
2. Secret trigger: PASS/FAIL
3. Storage upload: PASS/FAIL
4. Retention: PASS/FAIL
5. Restore test: PASS/FAIL
6. FINAL RESULT: PASS/FAIL

---

# CRITICAL RULES

* Do NOT echo BACKUP_SECRET
* If any step fails → STOP and report
* No code changes during validation

---

END
