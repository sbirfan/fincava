# CC-1 Gap Triage — Post-Replit Analysis
**Date:** 2026-05-04  
**Source:** FINCAVA_Complianc_Engine_SOT_CC1_CC1B_Analysis.docx (Replit)  
**Status:** Corrected prompt produced → CC1B_Replit_Paste_v2.md

---

## Verdict Summary

Replit confirmed the architecture is correct. The additive layer decision, priority queue formula, ICA five-state model, and dual success metric are all sound. The gaps are execution-level — wrong paths, wrong column names, missing functions. None require architectural revision.

**24 total issues: 10 critical (build failures) · 4 high (logic errors) · 6 medium (convention) · 4 low (completeness)**

---

## Blockers Before ANY CC-1B Execution

These must be answered before Replit runs a single line of CC-1B:

| # | Question | Owner | Resolution |
|---|----------|-------|------------|
| B1 | **Is CC-1A built?** scoring-service.ts writes to ai_outputs but does NOT parse compliance_gaps back into compliance_docs booleans. Graduation reads compliance_docs directly. If CC-1A is absent, all of CC-1B+ is blocked. | Irfan to verify in Replit | Must scope CC-1A into queue if missing |
| B2 | **Where is product_category stored?** ICA and FNC both gate on product_category. Currently lives in free-text supplierType/customSupplierType — not enum-controlled. supplier_export_mode table handles the declaration but the source category is undefined. | Replit codebase check | Define canonical storage before CC-1D |
| B3 | **What is req.user shape for supplier sessions?** CC-1D routes use req.user.supplierId but session auth populates req.user with the user record (not supplier). Need the actual join pattern the codebase uses. | Replit codebase check | Step 0 of corrected prompt covers this |

---

## Critical Fixes Applied in CC1B_Replit_Paste_v2.md

All 10 critical issues corrected:

| # | Issue | Fix Applied |
|---|-------|-------------|
| C1 | All file paths wrong | Corrected to monorepo-actual paths (see table below) |
| C2 | No DDL startup block in index.ts | Prompt specifies exact insertion point |
| C3 | Schema must go in lib/db/src/schema/ not db/schema.ts | New file: compliance-concierge.ts + index.ts export |
| C4 | overall_score column doesn't exist | → commercial_score |
| C5 | graduation_status column doesn't exist | → sellable_status; farm_name → inspect step |
| C6 | requireSupplier middleware doesn't exist | → requireAuth + inline supplier lookup |
| C7 | sendSupportRequestEmail doesn't exist | → create stub using existing email helper pattern |
| C8 | uploadMiddleware undefined | → use objectStorage.ts presigned URL pattern |
| C9 | fs/path not imported in index.ts | → add imports in wiring step |
| C10 | sql.raw needs explicit import | → import sql from drizzle-orm in wiring step |

**Correct path mapping:**

| Prompt Said | Actual Path |
|-------------|-------------|
| server/index.ts | artifacts/api-server/src/index.ts |
| server/routes/admin.ts | artifacts/api-server/src/routes/admin.ts |
| server/routes/supplier.ts | artifacts/api-server/src/routes/suppliers.ts |
| db/schema.ts | lib/db/src/schema/compliance-concierge.ts (new) |
| db/migrations/ | lib/db/migrations/ (create dir) |
| client/src/pages/ | artifacts/fincava/src/pages/ |
| client/src/components/ | artifacts/fincava/src/components/ |

---

## High Issues Fixed in CC1B_Replit_Paste_v2.md

| # | Issue | Fix |
|---|-------|-----|
| H1 | has_rut is not a valid state | Mapped to self_serve_in_progress (upload-ready) |
| H2 | Review endpoint has no state transition guard | Added allowlist: defined valid (from→to) pairs |
| H3 | Review endpoint doesn't check row exists before update | Added existence check with 404 |
| H4 | req.user.supplierId shape unconfirmed | Step 0 inspection + inline supplier ID derivation |

---

## Medium Issues Fixed

| # | Issue | Fix |
|---|-------|-----|
| M1 | console.log banned; use pino | → logger.info from lib/logger.ts |
| M2 | Migration SQL path resolves incorrectly | → absolute path from __dirname corrected |
| M3 | New routes not registered in routes/index.ts | → explicit registration instruction added |
| M4 | pnpm dev wrong for Replit | → removed; replaced with Replit workflow restart |
| M5 | No Zod validation on request bodies | → Zod schemas added for all CC-1D endpoints |
| M6 | decision field unvalidated | → enum check against 5 valid values |

---

## Low Issues — Addressed

| # | Issue | Status |
|---|-------|--------|
| L1 | Queue hard-capped at 100, no pagination | Added OFFSET parameter to query |
| L2 | Empty state on widget | Added empty state instruction to CC-1E |
| L3 | compliance_enablement_flows has no seed data | Seed SQL added for DIAN_RUT Phase I steps |
| L4 | Drizzle uniqueIndex missing from schema | Added uniqueIndex() call |

---

## SoT-Level Gaps (Not Prompt Issues — Design Decisions Needed)

These require decisions from Irfan, not prompt fixes:

| # | Gap | Recommendation |
|---|-----|----------------|
| G1 | OCR provider not named | Decision: use existing file storage + admin manual review in Phase I. Defer OCR to Phase II. |
| G2 | Admin queue vs existing list UI conflict | Decision: add as new tab in admin nav, not replacing existing list |
| G3 | managed_service_cases in Phase I with no payment | Keep table (it's just a record), but Phase I behaviour = WhatsApp trigger only. No fee_status logic until Phase III. |
| G4 | Buyer visibility conflicts with existing Certifications tab | Decision: new buyer_visibility_signals gate applies to compliance badges only. Existing certifications tab untouched. |
| G5 | compliance_enablement_flows authoring surface | Phase I: seed via SQL. Admin editing surface = Phase II. |
| G6 | compliance_requirements filter condition unverified | Replit to check: does the table have a scope/country column? |
| G7 | Cross-state-machine trigger undefined | Phase I: independent systems. Re-evaluation trigger = Phase II explicitly. |
| G8 | Spanish-first without i18n infrastructure | Phase I: Spanish hardcoded. Key-value file extraction = Phase II. TODO comments required. |
| G9 | WhatsApp handoff underspecified | Phase I: POST to support route fires email to admin only. WhatsApp template = separate task. |
| G10 | No test strategy | Accept Criteria in prompt = the Phase I test bar. Full test suite = separate task. |
| G11 | Phase II boolean deprecation trigger | Trigger: when >50% active suppliers have ≥1 verified requirement row. Encode in TDD. |

---

## Next Steps

1. **Irfan confirms B1 (CC-1A built?)** — this is the highest-risk blocker
2. **Paste CC1B_Replit_Paste_v2.md into Replit** — Step 0 is a codebase inspection pass that resolves B2+B3 before writing any code
3. **After CC-1B passes** → paste CC-1C section from CC1_Replit_Prompt_v2.md
4. **G1–G11** → encode resolved decisions into TDD v2.4 (separate task)
